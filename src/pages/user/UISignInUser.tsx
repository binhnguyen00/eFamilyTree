import React from "react";
import { Button, Stack } from "zmp-ui";

export default function UISignInUser() {
  return (
    <Stack space="1rem">
      <Button>
        đăng nhập
      </Button>
      <Button>
        đăng kí tài khoản
      </Button>
      <Button>
        đăng kí dòng họ
      </Button>
    </Stack>
  )
}