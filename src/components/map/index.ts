import config from "./config";
import { WorldMap } from "./WorldMap";

import { MapTile, MapCoordinate, MapMarker } from "types";

export { WorldMap, config as WorldMapConfig }
export type { 
  MapCoordinate, MapMarker, MapTile 
}